# Free-threaded override (PEP 779, https://peps.python.org/pep-0779/): the API runs
# under CPython 3.14t, no GIL.
# Usage: docker compose -f compose.yaml -f compose.ft.yaml up --build
services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile.ft
